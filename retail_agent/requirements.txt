# Must match the locally installed ADK version exactly.
# Mismatch between local and container ADK causes:
#   RuntimeError: coroutine raised StopIteration  (ADK <1.4.2)
#   Exception in ASGI application / empty stream  (version drift)
#
# Run: python3 -c "import importlib.metadata; print(importlib.metadata.version('google-adk'))"
# to get your local version, then set it here.
google-adk[a2a]==2.2.0
google-cloud-aiplatform[adk,agent_engines]>=1.95.0
